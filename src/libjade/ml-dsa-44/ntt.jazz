require "ntt.jinc"

export fn JASMIN_MLDSA44_mul_montgomery_reduce(reg u32 x y) -> reg u32
{
  reg u32 r;
  r = __mul_montgomery_reduce_8380417(x, y);
  return r;
}

export fn JASMIN_MLDSA44_ntt(reg ptr u32[N] rpa) -> reg ptr u32[N] {
  rpa = rpa;
  rpa = __ntt(rpa);
  return rpa;
}

export fn JASMIN_MLDSA44_invntt_tomont(reg ptr u32[N] rpa) -> reg ptr u32[N] {
  rpa = rpa;
  rpa = __invntt_tomont(rpa);
  return rpa;
}
